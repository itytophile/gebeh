{
  "author": "You <you@example.com>",
  "name": "rust-webpack-template",
  "version": "0.1.0",
  "scripts": {
    "build": "rimraf dist pkg && webpack --disable-interpret",
    "start": "rimraf dist pkg && webpack-dev-server --disable-interpret --open -d eval-source-map",
    "test": "cargo test && wasm-pack test --headless"
  },
  "devDependencies": {
    "@wasm-tool/wasm-pack-plugin": "^1",
    "copy-webpack-plugin": "^13",
    "rimraf": "^6",
    "ts-loader": "^9.5.4",
    "typescript": "^5.9.3",
    "webpack": "^5",
    "webpack-cli": "^6",
    "webpack-dev-server": "^5"
  }
}
